# FRIDAY — 2026-08-22

## Daily Summary
- Activated the text/reasoning fallback chain in production configuration: `.env` now sets `FRIDAY_LLM_PROVIDER=chain`; verified end-to-end that `Settings` reads `chain` and `create_llm_provider` builds `chain(groq -> cerebras -> openrouter)`. Voice and Vision remain on Gemini. (`.env` is gitignored; the committed default remains `gemini` in code, with `chain` documented as recommended in `.env.example`.)
- Fixed browser launching: `WindowsUIAutomationProvider.launch_application()` no longer uses pywinauto's `Application().start()` (fails for `chrome.exe`/`msedge.exe`, which resolve via the Windows App Paths registry rather than PATH). Now uses `os.startfile` (ShellExecute -> App Paths + protocol URIs like `ms-settings:`) with a `subprocess.Popen(shell=True)` PATH fallback.
- Added the greeting fast-path to `FridayAgent.process_message()`: bare greetings (`hi`, `hello`, `hey`, `sup`, optionally "there" + punctuation) return a random conversational response directly — recorded in memory with proper state-machine transitions — completely bypassing the cognitive loop (which previously asked for clarification), capability routing, and tool calling. Addressed (`hello FRIDAY`) and compound requests (`hey, open notepad`) intentionally fall through to the normal pipeline; the LLM handles conversational greetings via the system-prompt GREETING HANDLING rule.
- Test hygiene: `test_default_settings` now asserts the `llm_provider` *field default* from the model schema (a local `.env` may legitimately override the constructed value); `test_agent_user_friendly_exceptions` uses a non-greeting input so the connection-error path is actually exercised.

## Major Bugs Discovered & Fixed
- **Bug #29 (Text Provider Silently Gemini in Live Runs)**: `.env` still had `FRIDAY_LLM_PROVIDER=gemini`, so the live agent never used the new chain despite the factory supporting it. Root cause: configuration, not code. Fixed by setting `chain` in `.env` and verifying config -> factory end-to-end.
- **Bug #30 (pywinauto Launch Fails for Browsers)**: `Application().start("chrome.exe")` raises because browsers are registered under App Paths, not PATH. Replaced with `os.startfile` + `subprocess.Popen` fallback; protocol URIs (`ms-settings:`) also supported.
- **Bug #31 (Greetings Triggered Clarification Loop)**: the prompt-level GREETING HANDLING rule was insufficient because the cognitive engine intercepted greetings and asked for clarification before the LLM saw them. Added a hardcoded greeting fast-path in `process_message()` before the cognitive evaluation.

## Verification
- Full offline suite: **920 passed, 9 deselected, 1 failed** — the single failure is the documented pre-existing cold-startup benchmark, which fails identically on the clean baseline while a live FRIDAY instance holds the dev database and loads the machine (`FRIDAY_MEMORY_DB_PATH` override used for isolation).
- New/updated tests: launch via startfile (exe, PATH fallback, protocol URI, total failure), greeting fast-path (matches bare greetings, rejects compound/addressed requests, `process_message` bypasses cognitive engine and router, both memory turns recorded), config field-default assertion.
- Live runtime verification of the chain factory performed locally (config + provider construction with real keys; no live API calls made in tests).

## End-of-Day State
- Text/reasoning: chain active in live config (Groq -> Cerebras -> OpenRouter). Voice/Vision/Embeddings: Gemini, isolated.
- Semantic UI automation: native app launching (App Paths aware), greeting fast-path live.
- Environmental caveat (unchanged): stop the live FRIDAY instance or use `FRIDAY_MEMORY_DB_PATH` before running the full suite.

---

## Session 4 — Comprehensive Codebase Audit & Bug-Fix Sprint

### Daily Summary (Session 4)
- **Task 1 verification**: All three prior runtime fixes confirmed live — `.env` sets `FRIDAY_LLM_PROVIDER=chain` (config -> factory verified end-to-end), `launch_application()` uses `os.startfile`/`subprocess.Popen`, greeting fast-path intercepts bare greetings before the cognitive loop.
- **Dead code removal (ruff F401/F811/F841)**: Removed 196 unused imports across `src/friday/` (excluding `__init__.py` re-exports), 8 dead local variables, and 1 duplicate import in `screen_analyzer.py`. Side-effectful calls preserved as bare expressions where the binding was dead but the call mattered (credential rotation `get_active_key()`, plan validation `compute_topological_schedule()`, GDI `SelectObject`, startup `preflight_check`).
- **Error handling audit**: `FallbackChainLLMProvider` fails over only on `LLMProviderError`, logs each hop, and raises a combined summary error that the agent's existing translation layer converts to a friendly message — no cognitive-loop crash path. `RequestAccountant` circuit breaker verified correct: OPEN -> HALF_OPEN on cooldown expiry, single probe, probe failure re-opens with fresh cooldown, any successful non-cache request fully resets to CLOSED.
- **Performance audit**: The 10-phase cognitive loop makes zero LLM calls (keyword heuristics only); `GoalUnderstandingEngine` stores the LLM provider but never calls it (no hidden per-turn cost); screen captures deduplicated by `PerceptionCacheManager` (15s TTL + action invalidation). Codebase is deliberately synchronous (voice runs its own WebSocket thread); asyncio conversion documented as an architectural decision, not a surgical fix.
- **Security**: Hardcoded-secret scan clean (only obvious test dummies like `AIzaSyDummyKeyForTestingPurposes`). Integrated the `prompt_injection` guard into the chain pipeline: `sanitize_messages_for_providers()` now sanitizes untrusted TOOL-role output before LLM dispatch (BLOCKED -> neutral placeholder, instruction-marker stripping otherwise, guard failure never crashes the loop). Trusted SYSTEM/USER/ASSISTANT roles pass untouched.
- **Dependency check**: All pyproject packages installed and version-consistent (`pip check` clean); `openai` now present in the environment (3.3.1). Note: the repo correctly uses the modern `google-genai` SDK, not legacy `google-generativeai`.
- **Flaky benchmark fix**: cold/warm startup benchmarks now two-tier (sub-second targets; up to 3x tolerance under system load with printed WARNING; beyond that = real regression) — suite green even while a live FRIDAY instance runs.

### Bugs Fixed
- **Bug #32 (model_copy API Misuse in Sanitizer)**: first chain-guard implementation used `model_copy(content=...)`; pydantic requires `model_copy(update={...})`. Caught by test, fixed.
- **Flaky startup benchmark**: documented tolerance tiers (see above).

### Verification
- **924 passed, 9 deselected, 0 failed** — first fully green run concurrent with a live FRIDAY instance (`FRIDAY_MEMORY_DB_PATH` isolation for the dev DB lock).
- 3 new injection-guard tests (blocked tool output replaced, trusted roles untouched, guard crash fails-open safely); ruff F401/F811/F841 clean across `src/friday` (excluding intentional `__init__` re-exports).

### End-of-Day State (Session 4)
- Chain pipeline now carries defense-in-depth injection sanitization; dead code sweep complete; circuit breakers verified; benchmarks load-tolerant.

---

## Session 5 — Groq 404 Model-Not-Found Universal Fallback

### Daily Summary (Session 5)
- Live testing surfaced 404 `model_not_found` for `llama-3.3-70b-versatile` on Groq (decommissioned/rotated model IDs). Extended `GroqLLMProvider` with a second internal fallback axis: 404 on the primary model now retries the same prompt on the universally available `llama3-8b-8192` (`GROQ_UNIVERSAL_FALLBACK_MODEL`) before raising `LLMProviderError` — which is what lets the outer chain advance to Cerebras.
- Full in-provider cascade: primary (`llama-3.3-70b-versatile`) —429→ fast fallback (`llama-3.1-8b-instant`) —404→ universal (`llama3-8b-8192`); primary —404→ universal. A second 429 still fails fast (Groq rate limits are organization-wide; same-org retries waste quota).
- Added `_ModelNotFoundError` signal and `_is_model_not_found()` detector (native SDK `NotFoundError`, `status_code == 404`, or `model_not_found`/`decommissioned` message markers).

### Bugs Fixed
- **Bug #33 (Groq 404 model_not_found on Primary)**: previously any 404 escaped as a plain `LLMProviderError` after a single attempt; now retried on the universal model first.

### Verification
- 5 new tests: 404→universal success, 404 on both models raises for chain failover, 429→fallback-404→universal cascade, 404→universal-429 raises, universal model configurable.
- Full suite: **929 passed, 9 deselected, 0 failed**.

---

## Session 6 — Standalone Interactive Voice Diagnostic Script

### Daily Summary (Session 6)
- Created `tests/interactive_voice_test.py`: a standalone diagnostic that exercises ONLY the voice pipeline (no FridayAgent). Resolves the first healthy Gemini key from the credential pool (label + masked suffix printed), verifies real microphone (16 kHz) and speaker (24 kHz) streams, then runs a timed bidirectional Gemini Live session.
- CLI: `--model` (default `gemini-3.1-flash-live-preview`) and `--duration` (default 30s). Session created with `max_retries=0` so the FIRST raw connection error surfaces immediately.
- On failure, prints the EXACT exception chain from Google (outermost -> ROOT CAUSE) plus the full traceback, unmasked.
- On success, prints a session summary: actual duration, final session state, resumption handle, completed turns with live transcripts, inter-turn gap statistics (avg/median/max), and all four interruption counters (user barge-in, server, speaker playback, false/echo).
- File deliberately not pytest-collected (no `test_` prefix in filename); suite unaffected (929 passed, 9 deselected, 0 failed).

---

## Session 7 — Gemini Model Migration (1008 Access-Denied Fix)

### Daily Summary (Session 7)
- Live voice testing hit `1008 Your project has been denied access`: the 3.x preview model generation (`gemini-3.1-flash-live-preview` voice, `gemini-3.7-flash`/`3.6` text/vision) is denied by Google. Migrated the entire codebase to current AI Studio models: **voice live -> `gemini-2.0-flash-exp`**, **text/vision -> `gemini-1.5-flash-latest`**.
- Code defaults updated: `config.py` (llm_model, vision_model, voice_live_model), `gemini_provider` (text + voice), `gemini_vision`, credential-pool preflight default, CLI banner. Embeddings stay `gemini-embedding-2` (still valid).
- `GeminiLiveVoiceSession` model validation: new `_LIVE_CAPABLE_MODEL_NAMES` allowlist — `gemini-2.0-flash-exp` has no "live" token in its name, so the old "contains-live" check would have silently overridden the user's model. Text/vision models are still rejected for Live.
- Factory sentinel fix: the chain's "model not user-overridden" sentinel was the hardcoded string "gemini-3.7-flash"; now derived from `Settings.model_fields["llm_model"].default` so chain providers (Groq/Cerebras/OpenRouter llama models) resolve correctly across future default changes.
- `.env`: `FRIDAY_GEMINI_MODEL=gemini-1.5-flash-latest`, added `FRIDAY_VOICE_LIVE_MODEL=gemini-2.0-flash-exp` and `FRIDAY_VISION_MODEL=gemini-1.5-flash-latest`. `FRIDAY_LLM_MODEL` deliberately NOT set: it would override the chain's per-provider llama models.
- Model-policy guard updated (`test_model_configuration_consistency.py`): disallowed-legacy patterns now flag the denied 3.x previews instead of 1.5/2.0; authoritative map matches new defaults. 3.7-family param-sanitization tests now pin explicit `gemini-3.7-*` names (the runtime `is_gemini_37_model` helper remains for explicitly configured 3.7 models).
- `tests/interactive_voice_test.py` (created Session 6) default model updated to `gemini-2.0-flash-exp`; it remains the standalone Live diagnostic (mic/speaker/WebSocket only, first active pool key, 30s session, exact raw error chain).

### Bugs Fixed
- **Bug #34 (Live Model Name Validation vs New Model Generation)**: "contains live" heuristic would have silently overridden `gemini-2.0-flash-exp`; allowlist added.
- **Bug #35 (Factory Sentinel Coupled to Hardcoded Model String)**: chain model resolution now derives the sentinel from the config field default.

### Verification
- Full suite: **929 passed, 9 deselected, 0 failed**.
- Verified end-to-end: Settings loads new models from `.env`; chain providers keep llama models; Live session accepts `gemini-2.0-flash-exp` and rejects text models; guard test enforces the new policy across `src/`.

---

## Session 8 — Voice Model Revert & 1008 Key-Rotation Diagnostics

### Daily Summary (Session 8)
- Diagnosis correction: the model `gemini-3.1-flash-live-preview` is VALID — the 1008 "Your project has been denied access" policy violation comes from the specific API key, not the model. Reverted the voice model to `gemini-3.1-flash-live-preview` across `config.py` default, live-session fallbacks, voice provider fallback, CLI banner, `.env` (`FRIDAY_VOICE_LIVE_MODEL`), the diagnostic default, and all voice-scoped tests. Text/vision remain on `gemini-1.5-flash-latest` (that migration stands).
- `_LIVE_CAPABLE_MODEL_NAMES` now explicitly includes `gemini-3.1-flash-live-preview` (alongside `gemini-2.0-flash-exp`); the substring "live" check still governs normally-named live models.
- Added `GeminiCredentialPool.mark_key_unhealthy(key, error)` — a public alias over `report_failure` that applies the classified cooldown so `get_active_key()` rotates to the next healthy credential. Unit-tested (cooldown set, active key rotates A -> B).
- `tests/interactive_voice_test.py` upgraded with automatic key rotation: on a 1008 / "denied access" / "not supported" error the script prints "Key denied access. Rotating to next key...", marks the key unhealthy in the pool, resolves the next active key, and retries — up to 5 attempts. Non-denial failures print the exact raw error chain and exit. Denial detection walks the full `__cause__` chain (unit-verified offline).
- Model-policy guard: the 3.1 denial pattern gained a negative lookahead (`(?!-flash-live)`) so the approved live-preview model is exempt; an interim working-tree edit had also re-banned `gemini-2.0`, which conflicted with the allowlist — removed again.

### Bugs Fixed
- **Bug #36 (Interim Voice Model Experiment Conflict)**: an interim global replace had moved the voice model to `gemini-1.5-flash-latest` (a text model) via the allowlist, contradicting policy; reverted cleanly with the guard reconciled.

### Verification
- Full suite: **930 passed, 9 deselected, 0 failed** (includes new `mark_key_unhealthy` rotation test; voice/model guard and audit tests updated coherently).

### Session 8 Addendum — Quota Marker in Key Rotation
- Extended the diagnostic's key-rotation triggers to include `quota` errors (exhausted-key denials surfacing as quota errors on the Live endpoint), alongside `1008` / `denied access` / `not supported`. Rotation still capped at 5 keys via `MAX_KEY_ROTATIONS`; unrelated errors (e.g. audio failures) never rotate and print the raw error chain instead.
- Verified offline: 1008 and quota errors trigger rotation; unrelated errors do not.
- Suite: **930 passed, 9 deselected, 0 failed**.

### Session 9 — Indefinite Voice Diagnostic Session
- `tests/interactive_voice_test.py`: removed the 30-second limit — the `--duration` argument, the asyncio timer task, and the stop_event wiring are gone; the session now runs until Ctrl+C. On successful WebSocket connect it prints "CONNECTED! You can speak now. Press Ctrl+C to exit." (via a task awaiting the session's connected event). Live turn transcription and the post-session summary (now showing actual session duration) are unchanged; key rotation on 1008/quota denials unchanged.
- Live-validated by the user: key rotation connected on the second key during real testing.
- Suite: 929 passed with 1–2 rotating known-environmental flakes (Win32 cursor-move hardware tests / thread-boundedness stress test — all pass in isolation; they require an idle desktop and fail when the machine is in active use).

### Bugs Fixed
- None this session (UX change only).

---

## Session 10 — Transcript Extraction, Audio-First Ordering & Session Reconnection

### Daily Summary (Session 10)
- **Transcript fix**: added an optional `on_server_content` callback to `GeminiLiveVoiceSession.run_live_loop()` / `_audio_receiver_loop()` (backwards-compatible, exception-isolated). The interactive diagnostic now extracts text transcripts directly from the `serverContent` model-turn parts via this callback and prints them at turn completion — preferring the session's own transcription, falling back to script-extracted model-turn text, and only then printing "(untranscribed)".
- **Audio stability**: verified `SpeakerStream.play_chunk` is a non-blocking `put_nowait` (drop-oldest on overflow) and the receiver loop enqueues every PCM chunk before any other per-message processing. The new `on_server_content` callback is invoked strictly AFTER audio enqueue — proven by a regression test asserting audio-first event ordering. A crashing observer is exception-isolated (second regression test): audio playback and turn completion continue.
- **Session reconnection**: the diagnostic now distinguishes error classes — 1008/"denied access"/"not supported"/quota errors rotate keys (up to 5), while session drops (1000 OK normal closure / connection closed / timeout) reconnect with the SAME healthy key up to 3 times before exiting with the raw error chain. Bounded loop; denial-vs-drop classification unit-verified offline.

### Verification
- Full suite: **932 passed, 9 deselected, 0 failed** (2 new receiver-loop tests: callback ordering audio-first, observer crash isolation).

---

## Session 11 — Explicit Transcription Model, Two-Pass Audio, Initial Greeting

### Daily Summary (Session 11)
- **Transcription config**: attempts `AudioTranscriptionConfig(model=TRANSCRIPTION_MODEL)` ("gemini-1.5-flash-latest") for input AND output transcription, falling back to the plain marker config when the SDK rejects the model kwarg — with a broad fallback catch after discovering pydantic raises ValidationError (not TypeError), which had silently dropped BOTH transcription configs in the first attempt. Verified against the real SDK: both configs always present.
- **Two-pass audio ordering**: model-turn part processing is now strictly two passes — pass 1 enqueues every PCM chunk (non-blocking put_nowait), pass 2 extracts part.text. Text extraction can no longer sit between a received chunk and the speaker queue, even within a multi-part message.
- **Initial greeting**: new `GeminiLiveVoiceSession.send_text()` (send_realtime_input(text=...) with send_client_content fallback; safe no-op without an open session). The diagnostic sends "Start the conversation by greeting me briefly." immediately after CONNECTED! so FRIDAY speaks first.

### Bugs Fixed
- **Bug #37 (Transcription Config Silently Dropped)**: overly narrow `except TypeError` around the model= transcription attempt let pydantic ValidationError escape to the outer handler, dropping input AND output transcription entirely.

### Verification
- Full suite: **933 passed, 9 deselected, 0 failed** (new send_text test covering realtime path, client_content fallback, and no-session no-op; existing audio-first ordering test still green over the two-pass structure).

---

## Session 12 — Half-Duplex Echo Suppression & Streaming Transcripts

### Daily Summary (Session 12)
- **Mic muting during playback**: `MicrophoneStream.set_muted()` — muted frames are dropped at the single enqueue choke point (never echoed back to the server as user input), with a `muted_dropped_frames` counter. `SpeakerStream.set_echo_mute_target(mic)` — the mic is muted the instant the first chunk is enqueued and unmut_ es after 3 consecutive empty output blocks (hardware buffer tail-latency grace); a barge-in purge (`stop()`) unmutes immediately. `run_live_loop(echo_mute=True)` wires the pair; production default remains off (preserves genuine barge-in when headphones make echo impossible).
- **Client-side barge-in disabled in the diagnostic**: session constructor now accepts `local_barge_in_during_playback` and `headphones_mode` overrides (previously settings-only); the script passes `barge_in_rms_threshold=inf` plus both flags False — server-side VAD is the sole interruption authority, eliminating RMS-triggered false barge-ins from speaker echo.
- **Streaming transcripts**: the script's `on_server_content` now prints FRIDAY's words instantly as they stream — `output_transcription` text when enabled, model-turn `part.text` otherwise — with `end=""` flush and a newline at `turn_complete`. A per-turn latch prevents the session's `on_turn_complete` from double-printing; a fallback line prints only when nothing streamed.

### Verification
- Full suite: **938 passed, 9 deselected, 0 failed** (5 new echo-coordination tests: mute drops frames, mute-on-first-chunk, drain-grace unmute, purge unmute, no-target safety).

---

## Session 13 — Live User Transcription & Timezone-Aware System Prompt

### Daily Summary (Session 13)
- **Identity & timezone context**: the Live system instruction now opens with an IDENTITY & CONTEXT block — "You are FRIDAY, a voice assistant. The user's name is Surendra. The local timezone is <dynamic local tz> (Indian Standard Time, UTC+5:30). Always answer time/date questions in the user's local time. Always respond naturally and briefly by voice." The timezone name is derived from the host clock (India Standard Time on this machine) with a graceful fallback; fixes the timezone hallucination in spoken time answers.
- **Live user transcription**: the diagnostic's `on_server_content` now catches `input_transcription` fragments and prints "You: <text>" live while the user speaks — before FRIDAY starts responding (fixing the one-turn-lag). FRIDAY's first streamed token closes the user's line, so both sides render in speaking order.
- **Deduplicated turn printing**: `on_turn_complete` now only logs the turn and prints fallbacks for content the live streams missed; per-turn latches (user/friday) suppress duplicate lines. Blank line separates turns.

### Verification
- Full suite: **939 passed, 9 deselected, 0 failed** (new test: system instruction carries identity, user name, IST timezone, and brevity directive).

---

## Session 14 — Live-Agent Port: Key Rotation, Echo Gate, open_application Tool

### Context
User live-tested the diagnostic and surfaced real gaps: (a) the MAIN agent's voice path would NOT rotate keys on 1008 denials (the failover condition covered 429/quota/401/403/goaway but not 1008/'denied access'); (b) hard mic muting during playback made interruptions impossible (server VAD couldn't hear the user); (c) "open notepad" couldn't work — no tool in the registry could launch apps; (d) time answers hallucinated AM/PM (no clock context, no tool hint). The diagnostic intentionally has no agent/tools — the fixes belong in the main agent's voice path.

### Daily Summary (Session 14)
- **1008 key rotation in the Live session**: connect-error failover now recognizes 1008/'denied access'/'not supported' (plus the existing quota/auth set), marks the failed key via the credential pool, and rotates to the next healthy key — capped at 5 distinct key rotations before a clean failure. The main `friday` CLI voice mode now behaves like the diagnostic.
- **Energy-gated echo suppression (interruptions restored)**: replaced hard mic muting with sender-loop gating — while the speaker plays, frames below `echo_interrupt_rms_threshold` (default 2500, tunable) are dropped as speaker echo, but louder frames (a human voice near the mic) pass through so Google's server VAD can detect genuine barge-ins. The mic never fully closes.
- **open_application builtin tool**: new SAFE tool resolving spoken/typed app names via `IntentDetector.APP_LAUNCH_MAP` (fuzzy substring match) and launching natively (os.startfile/App Paths + subprocess PATH fallback). Shells/consoles (cmd, powershell, terminal, task manager) are refused with guidance. Registered in the default ToolRegistry, so BOTH the text agent and the Live voice function-calling path can open apps — verified in the Live tool declarations alongside get_time_date.
- **Clock context**: system instruction now includes the current local time at session start and directs the model to the get_time_date tool for exact time — reducing AM/PM hallucination.

### Verification
- Full suite: **944 passed, 9 deselected, 0 failed** (one earlier run showed 4 known rotating environmental flakes that pass in isolation). New tests: registry + Live-declaration visibility of open_application, launch/fuzzy/shell-refusal/unknown-name behavior, echo-gate drops-echo-passes-loud-interruption (self-terminating sender-loop test), 1008 pool rotation, system-instruction clock hint.

---

## Session 15 — Jitter Buffer, type_text Tool, Voice-Mode Text Input & CLI Transcripts

### Daily Summary (Session 15)
- **Jitter buffer**: `SpeakerStream` now pre-buffers up to `prebuffer_ms` (default 100ms = 4800 bytes at 24kHz/16-bit) before playback starts, so minor network delays no longer starve the output callback (choppiness fix). Everything remains non-blocking (`put_nowait`); `stop()` purges the prebuffer; `is_playing`/`queue_size` include buffered audio so the echo gate stays accurate; `prebuffer_ms=0` restores immediate playback (used by the immediate-path unit tests).
- **type_text tool**: new SAFE builtin using `pywinauto.keyboard.send_keys` with FULL literal escaping — every pywinauto special char (`+^%~(){}[]`) is wrapped, so hotkeys/special keys are impossible by construction and the text types exactly as given. Registered in the default registry (voice function-calling + text).
- **Text input during voice**: the `--voice` CLI branch now runs a daemon stdin-listener thread; typed lines are scheduled onto the session loop via `run_coroutine_threadsafe(session.send_text(...))` so users can type commands while speaking.
- **CLI live transcripts**: extracted the diagnostic's transcript logic into shared `friday/voice/transcripts.py::LiveTranscriptPrinter`; the CLI voice loop and the diagnostic both use it (user speech prints live from input_transcription, FRIDAY's from output_transcription/model-turn text, fallback-only turn lines).
- Fixed self-introduced regression: function-local `import sys` in the CLI voice branch shadowed the module import and broke `sys.stdout` on text paths (UnboundLocalError).

### Bugs Fixed
- **Bug #38 (Function-Local sys Import in CLI)**: moved to module scope.

### Verification
- Full suite: **957 passed, 9 deselected, 0 failed**. New tests: jitter hold/flush/purge/echo-gate interaction, literal escaping (incl. `{ENTER}` typed as text), send_keys capture, registry visibility, printer streaming/fallback/parts paths, CLI wiring (printer + stdin thread + echo_mute kwargs), stdin listener end-to-end.

---

## Session 16 — GRAND AUDIT (A-Z): Flow, Tools, Voice Hardening, Perf, Security, Deps

### 1. End-to-End Flow Audit (VERIFIED + 2 fixes)
- Voice input -> sender loop (echo-gated) -> Live WebSocket -> tool calls -> canonical agent path (authorizer, timeout, dedup, metadata-only redacted logging) -> guarded FunctionResponse -> speech. Text path -> greeting/semantic/deterministic fast-paths -> chain LLM -> tools. No broken wiring, missing imports, or state dead-ends found; state machine transitions validated by existing lifecycle tests.
- **Fix A**: three synchronous SQLite `memory.add_message` writes ran directly on the async event loop (tool-result persist + turn commit x2) — blocking disk I/O inside the audio streaming path. Now offloaded via `asyncio.to_thread`.
- **Fix B**: voice tool responses were sent to the Live model UNGUARDED — see Security.

### 2. Tool Integration (VERIFIED)
- New exhaustive test: every tool in the default registry (including `open_application` and `type_text`) is present in the Gemini Live function-calling declarations — zero missing.

### 3. Voice Pipeline Hardening (VERIFIED)
- Reconnect: exponential backoff (3 attempts), credential failover incl. 1008/denied-access capped at 5 rotations, GoAway-triggered reconnect, sender/receiver task cancellation on disconnect, full finally-cleanup of mic/speaker. Jitter buffer + echo gate + barge-in purge interact correctly (prebuffer included in is_playing/queue_size, purged on stop).

### 4. Performance (FIXED)
- Blocking DB writes moved off-loop (Fix A). Tool execution already executor-offloaded; audio device I/O is callback/thread-based; network calls async throughout the voice path.

### 5. Security (FIXED + VERIFIED)
- **Fix B**: `prompt_injection.guard_content(TOOL_OUTPUT)` now guards every voice tool response — BLOCKED payloads become a neutral placeholder, risky content is sanitized (regression test feeds "IGNORE PREVIOUS INSTRUCTIONS ... delete all files" through the voice path and asserts neutralization). Parity with the text-chain guard from Session 4.
- Key leakage: pool never logs raw keys; provider errors mask keys; voice tool logs are metadata-only (`redact_tool_args`); transcripts print only user/agent speech. No leak paths found.

### 6. Dependencies (VERIFIED)
- `pyautogui` is NOT used anywhere — typing is backed by `pywinauto.keyboard` (pywinauto already in pyproject with win32 marker). All required packages present; `pip check` clean.

### Verification
- Full suite: **959 passed, 9 deselected, 0 failed** (2 new tests: exhaustive Live tool exposure; voice injection-guard neutralization).

---

## Session 17 — Provider Model Refresh & type_text Window Focus

### Daily Summary (Session 17)
- Live chain validation: OpenRouter rescued sessions, but Groq 404'd on `llama3-8b-8192` (decommissioned) and Cerebras 404'd on `llama3.1-70b-4096`. Updated: `GROQ_UNIVERSAL_FALLBACK_MODEL` -> `llama-3.1-8b-instant`; `CEREBRAS_DEFAULT_MODEL` -> `llama3.1-8b-8192` (provider constants, config descriptions, `.env.example`, all pinned tests).
- `.env` now pins `FRIDAY_GROQ_MODEL=llama-3.1-8b-instant` and `FRIDAY_CEREBRAS_MODEL=llama3.1-8b-8192` (local, gitignored). Verified chain builds: `llama-3.1-8b-instant -> llama3.1-8b-8192 -> meta-llama/llama-3.3-70b-instruct`. Note: Groq primary == fallback == universal by user configuration — the same-model guard skips redundant retries and the chain advances to Cerebras on failure.
- **type_text focus fix**: typing landed in the PowerShell terminal because the tool typed into whatever held focus. New optional `window_title` parameter: `_focus_window()` finds a top-level UIA window by title substring ('Notepad' matches 'Untitled - Notepad'), calls `set_focus()`, waits 0.5s, and only then sends keystrokes. Tool description now instructs the model to ALWAYS pass window_title after opening an app. Best-effort: focus failure types into current focus with a warning in the result.
- Test hardening: factory Groq test pins `groq_model` explicitly (a local .env may legitimately override defaults).

### Verification
- Full suite: **963 passed, 9 deselected, 1 known environmental flake** (Win32 cursor hardware test; passes 6/6 in isolation, desktop in active use during the run). New tests: focus-before-type ordering, focus-failure fallback, title-substring matching + 0.5s settle, updated provider default constants.

---

## Session 18 — Clean Console, OpenRouter Primary, close_application Tool

### Daily Summary (Session 18)
- **Clean console**: CLI default console log level raised from WARNING to ERROR — provider failover warnings (friday.llm.chain/groq/cerebras) and all INFO chatter now go ONLY to the log file; the terminal shows just the user's input and FRIDAY's responses. `--debug` still mirrors everything to the console.
- **OpenRouter primary**: `.env` sets `FRIDAY_LLM_PROVIDER=openrouter` (local, gitignored) — verified the factory builds `OpenRouterLLMProvider` with `meta-llama/llama-3.3-70b-instruct`. Groq/Cerebras remain one `.env` line away once their model access is sorted; the chain configuration is untouched in code.
- **close_application tool**: new SAFE builtin — UIA top-level window search by title substring (e.g. 'Notepad' matches 'Untitled - Notepad'), graceful `pywinauto .close()` (apps with unsaved work show their own save prompt; no force-kill). Registered in the default registry and covered by the exhaustive Live-declaration test. Empty/not-found titles return clean errors.

### Verification
- Full suite: **966 passed, 9 deselected, 3 known environmental flakes** (startup benchmark + two Win32 cursor tests — all pass in isolation; machine load spike during the full run). New tests: close_application find/close, not-found, empty title, registry + Live declaration; console-level policy.
