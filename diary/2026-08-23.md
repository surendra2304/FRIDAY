# FRIDAY — 2026-08-23

## Daily Summary
- Gemini Vision 404: Updated default vision model to `gemini-1.5-flash` and prevented cooldown lockouts on MODEL_NOT_FOUND.
- Prefer Local OCR: Updated prompts to prefer `read_screen_text` before `get_screen_snapshot`.
- Time Loop Fix: Enforced strict rule: "Do not state the time unless the user explicitly asks for it."
- Lingering Shutdown Task: Tracked `stop_task` and `greeting_task` and drained in finally blocks.
- Stable Audio Pipeline & Half-Duplex Echo Suppression: Reverted `FRIDAY_VOICE_HEADPHONES_MODE` to `false` in `.env` and `.env.example`. Re-enabled half-duplex echo suppression with `echo_mute=True`. Enabled high-threshold local barge-in (`RMS 4000.0`) in CLI so user shouting interrupts FRIDAY while steady ambient noise is safely ignored. Continuous microphone PCM audio dispatch to Gemini Live is preserved whenever the speaker is idle.
- Microphone Unmute Enforcement & Playback Timeout: Ensured `microphone.set_muted(False)` is called continuously while speaker is idle. Added a 10s playback hard timeout to break speaker deadlocks and force unmute. Added console debug logging `[DEBUG] Mic unmuted, listening...` upon transition from muted to unmuted.
- Lazy Imports & Fast Startup: Made heavy module-level dependencies (`numpy`, `resemblyzer`, `pytesseract`, `PIL`) lazy inside `voice_biometrics.py`, `screen_ocr.py`, `screen_snapshot.py`, and `detector.py`.
- Clean Audio Queue & Jitter Buffer: Verified `SpeakerStream` 100ms jitter prebuffer and confirmed no extraneous queue purges occur during playback.

## Verification
- pytest suite: 1011 passed, 4 skipped, 9 deselected in 155.96s.
