# FRIDAY Engineering Diary — 2026-08-23

## Daily Summary
- I fixed the Gemini Vision 404: I updated the default vision model to `gemini-1.5-flash` and prevented cooldown lockouts on MODEL_NOT_FOUND.
- I updated prompts to prefer `read_screen_text` before `get_screen_snapshot` (local OCR first).
- I added Time Loop & Anti-Repetition Rules in `src/friday/voice/gemini_live_session.py`: strict instructions "NEVER repeat greetings ('Hi Surendra', 'Hello', etc.). NEVER greet the user again after the session has started. NEVER state the time or date unless explicitly asked in the immediate query. Respond ONLY to the user's immediate query or command. NEVER summarize or recite past tool executions, past actions (like opening or closing apps), or previous conversation history unless explicitly asked." I cleaned the Live system prompt from injecting past tool execution history dumps.
- I tracked `stop_task` and `greeting_task` and drained them in finally blocks to fix lingering shutdown tasks.
- I completely disabled local client-side RMS barge-in checks in `src/friday/voice/gemini_live_session.py` and set `barge_in_rms_threshold=float('inf')` with `local_barge_in_during_playback=False` in `src/friday/cli/main.py`. Interruption authority now relies 100% on Google Server-Side VAD, eliminating playback mid-sentence cut-offs.
- I ensured `microphone.set_muted(False)` is called continuously while the speaker is idle. I added a 10s playback hard timeout to break speaker deadlocks and force unmute. I added console transition logging upon unmuting.
- I added deterministic direct fast-paths in `FridayAgent` for desktop launch/close (Chrome, Notepad, Calculator, Explorer, VS Code, Task Manager, Settings, Windows Update), exact time, laptop identity/specs queries, and listening checks. I integrated turn completion routing from Live transcript to local agent controller to ensure deterministic execution without LLM hallucinatory repeats.
- I made heavy module-level dependencies (`numpy`, `resemblyzer`, `pytesseract`, `PIL`) lazy inside `voice_biometrics.py`, `screen_ocr.py`, `screen_snapshot.py`, and `detector.py` for fast startup.
- I verified `SpeakerStream` 100ms jitter prebuffer and confirmed no extraneous queue purges occur during playback.

---

## Desktop Fast-Paths, Bug Sweep, and 100% Green Suite

- I added new Instant Fast Paths (`FridayAgent._direct_desktop_action_fast_path`): volume up/down/set/mute/unmute (pycaw, relative + absolute with clamping and no-op detection), battery status query (Win32 GetSystemPowerStatus, read-only), and screen description ("what's on my screen" -> ScreenSnapshotTool + Gemini Vision). All bypass the LLM for sub-second deterministic responses in text AND voice mode.
- I fixed pycaw 2025 Compatibility: `_get_endpoint_volume` now unwraps the new `AudioDevice` wrapper (`._dev`) before `Activate`, plus best-effort COM init; this fixes AttributeError on current pycaw releases.
- I applied Vision Model Migration: Google retired `gemini-1.5-flash` AND blocks `gemini-2.5-flash` for this key (404). I applied the API-recommended migration to `gemini-3.6-flash` across config default, `gemini_vision.py` fallback, `.env`, `.env.example`, and authoritative model tests; I removed the now-invalid legacy pattern.
- I fixed the Cognitive Loop: `cognitive.py` was calling the nonexistent `normalize_and_decompose()`. I updated it to call `GoalUnderstandingEngine.analyze_goal()` and use `clarification_needed`; goal understanding is live again.
- I fixed Scheduler issues: I added a missing `TaskRunLog` import; I removed a phantom voice notification calling the nonexistent `VoiceSession.speak()`; I replaced broken `authorize_sensitive/authorize_dangerous` calls with the canonical `AuthorizationRequest` flow.
- I fixed the ExecuteCommandTool Allowlist: `rstrip(".exe")` was corrupting `type`/`where`/`tree`; I replaced it with `removesuffix(".exe")`.
- I fixed the Center Resolution: unbound `WindowsScreenCaptureProvider.list_displays()` calls in `execute_computer_action.py` and `action_proposal.py` were always raising TypeError and silently falling back to hardcoded 960x540; I instantiated them properly and made them origin-aware.
- I fixed the Cerebras Model: corrected model id to `llama3.1-8b` in the provider constant, `.env`, and tests.
- I fixed Test Stability: physical-input routing tests now inject deterministic `MockWindowsInputDriver` like the center test.
- I created a new test file `tests/test_futuristic_fast_paths.py`: 10 tests covering volume math/clamp/no-op/mute delegation/error paths, battery routing, screen describe success+failure, and negative non-hijack cases.

### Verification
- Full suite: **1036 passed, 0 failed**, 9 deselected (live/hardware opt-in) in ~155s.
- Live hardware smoke on host: battery read OK; volume set/restore/mute/unmute OK via pycaw; real Gemini Vision screen description OK on `gemini-3.6-flash`.
