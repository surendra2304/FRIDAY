# FRIDAY — 2026-08-23

## Daily Summary
- Gemini Vision 404: Updated default vision model to `gemini-1.5-flash` and prevented cooldown lockouts on MODEL_NOT_FOUND.
- Prefer Local OCR: Updated prompts to prefer `read_screen_text` before `get_screen_snapshot`.
- Time Loop & Anti-Repetition Rules: In `src/friday/voice/gemini_live_session.py`, added strict instructions: "NEVER repeat greetings ('Hi Surendra', 'Hello', etc.). NEVER greet the user again after the session has started. NEVER state the time or date unless explicitly asked in the immediate query. Respond ONLY to the user's immediate query or command. NEVER summarize or recite past tool executions, past actions (like opening or closing apps), or previous conversation history unless explicitly asked." Cleaned Live system prompt from injecting past tool execution history dumps.
- Lingering Shutdown Task: Tracked `stop_task` and `greeting_task` and drained in finally blocks.
- 100% Server-Side VAD (Local Barge-In Disabled): Completely disabled local client-side RMS barge-in checks in `src/friday/voice/gemini_live_session.py` and set `barge_in_rms_threshold=float('inf')` with `local_barge_in_during_playback=False` in `src/friday/cli/main.py`. Interruption authority relies 100% on Google Server-Side VAD, eliminating playback mid-sentence cut-offs.
- Microphone Unmute Enforcement & Playback Timeout: Ensured `microphone.set_muted(False)` is called continuously while speaker is idle. Added a 10s playback hard timeout to break speaker deadlocks and force unmute. Added console transition logging upon unmuting.
- Direct Desktop Fast Paths & Local Action Engine: Added deterministic direct fast-paths in `FridayAgent` for desktop launch/close (Chrome, Notepad, Calculator, Explorer, VS Code, Task Manager, Settings, Windows Update), exact time, laptop identity/specs queries, and listening checks. Integrated turn completion routing from Live transcript to local agent controller to ensure deterministic execution without LLM hallucinatory repeats.
- Lazy Imports & Fast Startup: Made heavy module-level dependencies (`numpy`, `resemblyzer`, `pytesseract`, `PIL`) lazy inside `voice_biometrics.py`, `screen_ocr.py`, `screen_snapshot.py`, and `detector.py`.
- Clean Audio Queue & Jitter Buffer: Verified `SpeakerStream` 100ms jitter prebuffer and confirmed no extraneous queue purges occur during playback.

### Verification (earlier session)
- pytest suite: 1021 passed, 4 skipped, 9 deselected in 180.00s.

## Session 2 — Futuristic Upgrade, Bug Sweep, and 100% Green Suite
- New Instant Fast Paths (`FridayAgent._direct_desktop_action_fast_path`): volume up/down/set/mute/unmute (pycaw, relative + absolute with clamping and no-op detection), battery status query (Win32 GetSystemPowerStatus, read-only), and screen description ("what's on my screen" -> ScreenSnapshotTool + Gemini Vision). All bypass the LLM for sub-second deterministic responses in text AND voice mode.
- pycaw 2025 Compatibility: `_get_endpoint_volume` now unwraps the new `AudioDevice` wrapper (`._dev`) before `Activate`, plus best-effort COM init; fixes AttributeError on current pycaw releases.
- Vision Model Migration: Google retired `gemini-1.5-flash` AND blocks `gemini-2.5-flash` for this key (404). API-recommended migration to `gemini-3.6-flash` applied across config default, `gemini_vision.py` fallback, `.env`, `.env.example`, and authoritative model tests; removed the now-invalid `\bgemini-3\.6\b` legacy pattern.
- Cognitive Loop Fix: `cognitive.py` called nonexistent `normalize_and_decompose()` (AttributeError swallowed every turn). Now calls `GoalUnderstandingEngine.analyze_goal()` and uses `clarification_needed`; goal understanding is live again.
- Scheduler Fixes: missing `TaskRunLog` import (NameError after every run) added; phantom voice notification calling nonexistent `VoiceSession.speak()` removed; broken `authorize_sensitive/authorize_dangerous` calls replaced with canonical `AuthorizationRequest` flow.
- ExecuteCommandTool Allowlist Fix: `rstrip(".exe")` (character-set strip) corrupted `type`/`where`/`tree`; replaced with `removesuffix(".exe")`.
- Center Resolution Fix: unbound `WindowsScreenCaptureProvider.list_displays()` calls in `execute_computer_action.py` and `action_proposal.py` always raised TypeError and silently fell back to hardcoded 960x540; now instantiated properly and origin-aware.
- Cerebras Model Fix: default `llama3.1-8b-8192` is a Groq-style id rejected by Cerebras; corrected to `llama3.1-8b` in provider constant, `.env`, and tests.
- Test Stability: physical-input routing tests (corners/vision-outage) injected the deterministic MockWindowsInputDriver like the center test — removes live-desktop SetCursorPos races that flaked under suite load.
- New test file `tests/test_futuristic_fast_paths.py`: 10 tests covering volume math/clamp/no-op/mute delegation/error paths, battery routing, screen describe success+failure, and negative non-hijack cases.

### Verification (Session 2)
- Full suite: **1036 passed, 0 failed**, 9 deselected (live/hardware opt-in) in ~155s.
- Live hardware smoke on host: battery read OK; volume set/restore/mute/unmute OK via pycaw; real Gemini Vision screen description OK on `gemini-3.6-flash`.
