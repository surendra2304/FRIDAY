# FRIDAY — 2026-08-21

## Daily Summary
- Audited test suite for false confidence: eliminated mock claims, corrected test markers (`UNIT`, `INTEGRATION`, `SIMULATION`, `SECURITY`), and enforced signed capability requirements for authorizers.
- Upgraded provider independence: decoupled all core agent layers from `google.genai`, added `OpenAIVisionProvider` adapter, and validated offline execution with `MockLLMProvider` and `MockVisionProvider`.
- Upgraded background task engine: implemented `TaskSpec`, `TaskPersistenceStore` (SQLite), duplicate prevention, cancellation propagation, deadline tracking, and process crash recovery.
- Upgraded intelligence architecture: implemented 10-phase `CognitiveIntelligenceEngine` (`UNDERSTAND -> CLARIFY -> PLAN -> CHECK PLAN -> AUTHORIZE -> EXECUTE -> OBSERVE -> VERIFY -> LEARN -> COMPLETE`) with 5-dimension calibrated confidence scoring.
- Implemented multi-attribute `CapabilityRouter`: prioritized deterministic local computation, memory retrieval, and cached perception over external cloud APIs.
- Implemented unified domain error taxonomy: added 15-category `ErrorCode` hierarchy in `src/friday/core/exceptions.py` with automatic secret scrubbing and user/internal message separation.
- Built safe `FridayDoctor` subsystem: comprehensive health auditing for config, credential pool, audio devices, displays, SQLite database, task workers, and authorizers without secret leakage.
- Upgraded StepVerifier: added independent evidence verification across filesystem, running applications, screen state, structured JSON, and external services to detect tool false-success.
- Hardened memory trust boundary: added `TrustLevel.UNTRUSTED_EXTERNAL` classification and blocked external prompt injections from entering long-term vector embeddings.
- Stress-tested autonomous recovery and bounded loops: proved multi-step workflows recover from 429 quota exhaustion, screen drift, and halts upon budget exhaustion via 100-run Monte Carlo state machine simulation.
- Rebuilt performance benchmarks from scratch: measured cold startup (35ms), warm startup (3.2ms), RSS vs Tracemalloc heap, audio frame throughput (0.02ms), and sliding-window request budgets.
- Executed third-generation adversarial red-team audit: defended against privilege escalation, prompt injection, checkpoint tampering, and stale coordinate replay.
- Conducted real hardware diagnostics on Windows host: verified 17 physical microphone devices (1255.73 peak RMS), speaker output, and Gemini Live WebSocket session (`gemini-3.1-flash-live-preview`). Classified screen capture as `BLOCKED` under headless session.
- Passed full repository test suite: 844 passed, 4 skipped, 9 deselected in 144.46s (100% green pass rate).

## Major Architectural Upgrades
- **10-Phase Cognitive Intelligence Loop**: Structured phases with confidence thresholds (understanding < 0.65 triggers `CLARIFY`). Enforced that high confidence never bypasses authorization.
- **Evidence-Based Step Verifier**: Independent verification strategies that require real-world evidence (file on disk, window title, UI element) rather than trusting tool return values.
- **Memory Trust Boundary**: Explicit `TrustLevel` tagging on all messages; prevents untrusted tool outputs and web text from auto-embedding.
- **Capability Routing Layer**: Explicit selection of cheapest/safest execution mode (`DIRECT_REASONING`, `MEMORY_RETRIEVAL`, `LOCAL_COMPUTATION`, `LOCAL_SCREEN_INSPECTION`, `VISION_MODEL`, `VOICE_MODEL`, `TOOL_EXECUTION`, `BACKGROUND_TASK`).
- **Unified Error Taxonomy**: 15 distinct domain error categories with automatic secret scrubbing.
- **Durable Task Management**: SQLite-backed `TaskPersistenceStore` with restart crash recovery and checkpoint continuity.
- **Safe Request Accounting & Budgets**: Hierarchical limits (task, session, hourly, daily) and circuit breakers preventing quota-wasting loops.

## Major Bugs Discovered & Fixed
- **Bug #20 (Misleading Memory/CPU Benchmark Methodology)**: Replaced delta subtraction of instantaneous `psutil.cpu_percent` with interval sampling and explicit process RSS heap breakdowns.
- **Bug #21 (Redundant Screen Perception Loops)**: Built SHA-256 image byte hash caching (`PerceptionCache`) with TTL invalidation on action execution.
- **Bug #22 (Secret Leakage in Exception Strings)**: Scrubbed API keys and credentials in `FridayError.__str__()` and `FridayError.to_dict()` via `SecretScrubber`.
- **Bug #23 (Direct SDK Coupling in Core Layers)**: Decoupled tool registry, agent, and memory layers from `google.genai` SDK types; verified offline execution with mock providers.
- **Bug #24 (Background Task Crash Recovery Gap)**: Added durable SQLite state store and `recover_after_crash()` to restore unfinished tasks upon restart.
- **Bug #25 (Tool False-Success Discrepancy)**: Tools reporting success without modifying real-world state are detected by `StepVerifier` and marked `FAILED` or `UNVERIFIED`.
- **Bug #26 (Memory Trust Boundary Vulnerability)**: Tool results and external observations were auto-embedding without trust classification; patched with `TrustLevel.UNTRUSTED_EXTERNAL` filters.
- **Bug #27 (Unbounded Autonomous Loops)**: Execution engine now enforces strict per-step and global task limits, terminating in terminal `FAILED` rather than infinite retry cycles.

## Real-World Test Outcomes & Security Findings
- **Real Microphone Input**: `REAL PASS` (Realtek Audio Array captured 116 non-silent audio frames at 16kHz).
- **Real Speaker Output**: `REAL PASS` (Default speaker device index 3 ready for PCM audio; barge-in purge validated).
- **Real Gemini Live Voice**: `REAL PASS` (Full-duplex WebSocket session connected to `gemini-3.1-flash-live-preview`; 43,202 audio bytes received).
- **Real Cursor Control**: `REAL PASS` (Queried Windows cursor coordinates via ctypes Win32 API).
- **Screen Perception**: `BLOCKED` (Win32 BitBlt requires active interactive GUI desktop window station, blocked under headless session).
- **Cryptographic Authorization**: `SOFTWARE PASS` (HMAC-SHA256 capability tokens verified, Proposal != Execution invariant enforced).
- **Prompt Injection Defense**: `SOFTWARE PASS` (Red-team OCR, audio, and tool prompt injections isolated from plan execution and long-term memory).

## Final Repository State
- **Automated Test Suite**: 844 passed, 4 skipped, 9 deselected across 127 test files in 144.46s (100% green pass rate).
- **Static Analysis**: 108/108 source files AST verified (0 syntax errors, 0 raw secrets).
- **Release Status**: `SOFTWARE_VERIFIED` (100% software test suite passes offline).
- **Physical Hardware Status**: `PARTIAL_HARDWARE_BLOCKED` (Microphone and Speaker pass on physical hardware; Windows BitBlt GDI screen capture is blocked under non-interactive console context). Production-Ready claim is deferred until live GUI desktop execution.

---

## Session 2 — Text/Reasoning Provider Independence: Groq, Cerebras, OpenRouter & Cross-Provider Fallback Chain

### Daily Summary (Session 2)
- Achieved text/reasoning provider independence from Gemini: added `GroqLLMProvider`, `CerebrasLLMProvider`, and `OpenRouterLLMProvider`, all built on the OpenAI Python SDK (`openai` package) against each platform's OpenAI-compatible endpoint, inheriting from `BaseLLMProvider`.
- Implemented Groq internal model fallback: default `llama-3.3-70b-versatile`, automatic same-prompt retry on `llama-3.1-8b-instant` upon 429 `RateLimitError`.
- Built cross-provider automatic failover: new `FallbackChainLLMProvider` (`src/friday/llm/fallback_chain_provider.py`) tries providers in order (Groq -> Cerebras -> OpenRouter), advancing on any `LLMProviderError` (non-provider exceptions propagate immediately as bugs). Activated via `FRIDAY_LLM_PROVIDER=chain`.
- Expanded credential architecture: `GeminiCredentialPool` env loading parameterized (`env_key_names`/`env_fallback_fmts`); added non-singleton `OpenAICompatibleCredentialPool` with three independent module-level pools (`groq_credential_pool`, `cerebras_credential_pool`, `openrouter_credential_pool`), each with dedicated env vars and separate persisted health state files.
- Enforced strict voice/vision isolation: Gemini remains exclusively responsible for Voice (Gemini Live), Vision, and Embeddings. Voice pipeline (`voice/gemini_live_session.py`, `voice/gemini_provider.py`) imports only the Gemini `credential_pool` singleton; tests assert Groq/Cerebras/OpenRouter providers never consult the Gemini pool.
- Extended `Settings` (pydantic-settings): `groq_api_key`, `groq_model`, `groq_fallback_model`, `cerebras_api_key`, `cerebras_model`, `openrouter_api_key`, `openrouter_model` with `FRIDAY_*` and bare env aliases.
- Updated `llm/factory.py`: dedicated `groq`, `cerebras`, `openrouter`, and `chain` branches; `ConfigError` message lists all eight supported providers (`mock`, `openai`, `gemini`, `groq`, `openrouter`, `cerebras`, `chain`, `ollama`).
- End-to-end wiring verified: `FridayAgent` (`agent/agent.py`) consumes `create_llm_provider(settings)`, so `chain` flows transparently through the 10-phase cognitive loop, planner, and executor.

### Major Bugs Discovered & Fixed (Session 2)
- **Bug #28 (Global `time.sleep` Patch Contamination)**: `test_immediate_failover_on_429_quota_exhausted` patched `friday.llm.gemini_provider.time.sleep`, which patches the shared global `time` module — background threads leaked by earlier hardware tests (real Win32 `double_click` 0.05s sleeps) were recorded by the mock and tripped `assert_not_called()` (observed 37 foreign calls). Assertion now filters for provider retry-scale sleeps (>= 1.0s), preserving intent (zero backoff on quota exhaustion) while immune to unrelated threads.

### Verification (Session 2)
- Full offline suite green: **897 passed, 9 deselected** (`pytest -m "not live and not hardware" -q`), including 46 new mock tests across `test_groq_openrouter_providers.py`, `test_cerebras_provider.py`, and `test_fallback_chain_provider.py`.
- Import graph audit: no missing imports, no circular dependencies (`base <- groq <- {openrouter, cerebras}` single direction; factory imports all providers).
- Isolation audit: zero references to Groq/Cerebras/OpenRouter pools anywhere under `src/friday/voice/`.
- New providers are `SOFTWARE_VERIFIED` (mock-tested); first live validation requires `pip install openai` plus real keys in `.env`.

### End-of-Day State (Session 2)
- Text/reasoning chain: Groq 70B -> Groq 8B-instant (429 model fallback) -> Cerebras -> OpenRouter, then clean `LLMProviderError` with per-provider failure summary.
- Gemini remains exclusively Voice/Vision/Embeddings. `.env` secrets verified gitignored.

---

## Session 3 — Futuristic Computer Control Completion & Provider-Agnostic Identity

### Daily Summary (Session 3)
- Added `pywinauto>=0.6.8` (win32 marker) to `pyproject.toml` dependencies; verified installed (0.6.9) and importable.
- Completed semantic UI automation routing: `IntentDetector` now detects generic element clicks ("click the send button", "click the File menu", "click the General tab") and deterministic application launches ("open notepad" -> `notepad.exe`) for 17 known Windows apps, all at confidence 1.0 without touching the LLM or Vision pipeline.
- `WindowsUIAutomationProvider` gained `launch_application()` (pywinauto `Application().start()`) and improved `find_element()` scoring (per-part difflib ratio across name/automation_id/control_type plus substring containment bonus).
- `FridayAgent._execute_semantic_ui_action()`: consolidated semantic execution path; element clicks bypass LLM/Vision entirely; application launches are gated through the Authorizer (SAFE for GUI apps, SENSITIVE for shells/consoles), preserving the Proposal != Execution invariant.
- Fixed wiring bug: element-match confidence was previously compared against the intent regex confidence (always 1.0 for pattern hits), which fuzzy matching could essentially never reach, making the semantic path a dead end. Now gated by a fixed `UIA_ELEMENT_CONFIDENCE_THRESHOLD = 0.60`.
- Removed duplicated dead semantic-detection block nested inside the deterministic fast-path (unreachable copy-paste artifact).
- Identity prompt: added provider-agnostic IDENTITY section to the text system prompt ("multi-provider architecture for text, voice, and vision"; no single-vendor claims). Verified no "powered by Gemini"-style claims exist anywhere in source, README, or docs; voice persona untouched.
- Verification: 16 new mock tests (`test_semantic_ui_automation_routing.py`); full suite 912 passed / 1 pre-existing load-sensitive startup benchmark failure (fails identically on clean baseline while a live FRIDAY instance holds the dev database; passes when machine idle).

### End-of-Day State (Session 3)
- Semantic UI actions route: user phrase -> `IntentDetector` (regex, 1.0 confidence) -> pywinauto element click or authorized app launch -> direct response, zero LLM/Vision tokens.
- Known environmental caveat: running the FRIDAY CLI concurrently with `pytest` locks the shared `data/friday.db` (tests switch journal mode); use `FRIDAY_MEMORY_DB_PATH` override or stop the live instance before suite runs.

### Session 3 Addendum — Identity Wording & Greeting Rule
- Refined the system prompt identity to the requested second-person wording: "You are FRIDAY, a Fully Responsive Intelligent Digital Assistant. You operate using a multi-provider architecture for text, voice, and vision."
- Added GREETING HANDLING rule: simple greetings ('hi', 'hello', 'hey') get a natural response asking how to help; greetings are never treated as commands or tool targets.
- Extended prompt tests: exact identity wording asserted, greeting rule asserted, vendor-name ban maintained (18 tests in `test_semantic_ui_automation_routing.py`).
- Voice pipeline untouched (persona in `gemini_live_session.py` unchanged; Gemini pool isolation intact).
- Verification: 914 passed, 9 deselected, 1 pre-existing load-sensitive startup benchmark (documented above; fails identically on clean baseline while a live FRIDAY instance holds the dev database).
